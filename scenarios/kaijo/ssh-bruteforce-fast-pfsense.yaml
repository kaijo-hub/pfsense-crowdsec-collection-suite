type: leaky
name: kaijo/ssh-bruteforce-fast-pfsense
description: "Detect fast SSH bruteforce attempts on pfSense. Allows normal user mistakes (1–2 failed attempts) while still detecting fast brute-force patterns and noisy SSH activity."

filter: |
  evt.Meta.service == "ssh"
  && evt.Meta.log_type in [
    "ssh_failed_auth",
    "ssh_pam_auth_error",
    "ssh_invalid_user",
    "ssh_pre_auth",
    "ssh_permission_denied",
    "ssh_timeout_before_auth"
  ]

groupby: evt.Meta.source_ip
capacity: 6
leakspeed: 10s
blackhole: 1m
reprocess: true

labels:
  service: ssh
  remediation: true
  confidence: 3
  spoofable: 0
  classification:
    - attack.T1110
  behavior: "ssh:bruteforce"
  label: "SSH Bruteforce (pfSense)"
