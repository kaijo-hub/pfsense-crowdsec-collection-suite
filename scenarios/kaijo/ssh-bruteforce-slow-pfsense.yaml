type: leaky
name: kaijo/ssh-bruteforce-slow-pfsense
description: "Detect slow SSH bruteforce attempts on pfSense. Allows normal user mistakes (1–2 failed attempts) while still detecting fast brute-force patterns and noisy SSH activity."

filter: |
  evt.Meta.service == "ssh"
  && evt.Meta.log_type in [
    "ssh_failed_auth",
    "ssh_pam_auth_error",
    "ssh_invalid_user",
    "ssh_pre_auth",
    "ssh_permission_denied",
    "ssh_timeout_before_auth"
  ]

groupby: evt.Meta.source_ip
capacity: 12
leakspeed: 10m
blackhole: 30m
reprocess: true

labels:
  service: ssh
  remediation: true
  confidence: 3
  spoofable: 0
  classification:
    - attack.T1110
  behavior: "ssh:bruteforce"
  label: "SSH Slow Bruteforce (pfSense)"
